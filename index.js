const express = require("express");
const axios = require("axios");
require("dotenv").config();

const app = express();
app.use(express.json());

const PAGE_ACCESS_TOKEN = process.env.PAGE_ACCESS_TOKEN;
const VERIFY_TOKEN = process.env.VERIFY_TOKEN;
const CLAUDE_API_KEY = process.env.CLAUDE_API_KEY;
const CLAUDE_MODEL = process.env.CLAUDE_MODEL || "claude-sonnet-4-6";

const REQUIRED_ENV = { PAGE_ACCESS_TOKEN, VERIFY_TOKEN, CLAUDE_API_KEY };
for (const [key, value] of Object.entries(REQUIRED_ENV)) {
  if (!value) {
    console.error(`❌ Шаардлагатай env variable дутуу байна: ${key}`);
    process.exit(1);
  }
}

// ============================================================
// HEALTH CHECK
// ============================================================
app.get("/", (_req, res) => res.status(200).send("OK"));
app.get("/health", (_req, res) => res.status(200).json({ status: "ok" }));

// ============================================================
// 1. FACEBOOK WEBHOOK VERIFICATION
// ============================================================
app.get("/webhook", (req, res) => {
  const mode = req.query["hub.mode"];
  const token = req.query["hub.verify_token"];
  const challenge = req.query["hub.challenge"];

  if (mode === "subscribe" && token === VERIFY_TOKEN) {
    console.log("✅ Webhook амжилттай баталгаажлаа!");
    return res.status(200).send(challenge);
  }
  console.log("❌ Webhook баталгаажилт амжилтгүй болсон");
  return res.sendStatus(403);
});

// ============================================================
// 2. МЕССЕЖ ХҮЛЭЭН АВАХ ENDPOINT
// ============================================================
app.post("/webhook", async (req, res) => {
  // Facebook 20 секундийн дотор 200 хүлээдэг — хариуг шууд буцаагаад,
  // боловсруулалтыг арын background-д хийнэ.
  res.status(200).send("EVENT_RECEIVED");

  try {
    const body = req.body;
    if (body.object !== "page" || !Array.isArray(body.entry)) return;

    for (const entry of body.entry) {
      const events = Array.isArray(entry.messaging) ? entry.messaging : [];
      for (const event of events) {
        handleEvent(event).catch((err) =>
          console.error("❌ handleEvent алдаа:", err.message)
        );
      }
    }
  } catch (err) {
    console.error("❌ Webhook боловсруулалтын алдаа:", err.message);
  }
});

async function handleEvent(event) {
  const senderId = event?.sender?.id;
  if (!senderId) return;

  // Зөвхөн хэрэглэгчийн бичсэн текст мессежид хариулна.
  // delivery / read / echo events-ийг алгасна.
  if (event.delivery || event.read || event.message?.is_echo) return;
  const userMessage = event.message?.text;
  if (!userMessage) return;

  console.log(`📨 Мессеж ирлээ (${senderId}): "${userMessage}"`);

  await sendTypingIndicator(senderId);
  const reply = await getClaudeReply(userMessage);
  await sendMessage(senderId, reply);
}

// ============================================================
// 3. CLAUDE AI-ААС ХАРИУЛТ АВАХ
// ============================================================
const SYSTEM_PROMPT = `Та "Электронжаал" бүрэн дунд сургуулийн албан ёсны AI туслах.

Эцэг эх, сурагч, төгсөгч, иргэдэд нөхөрсөг, эелдэг,
тусархуу байдлаар хариулна.

==================================================
ТАНЫ ҮНДСЭН ҮҮРЭГ
==================================================

A. ЕРӨНХИЙ МЭДЭЭЛЭЛ → Сургуулийн талаарх ерөнхий
   асуултад хариулж, шаардлагатай бол захиргааны
   ажилтанд дамжуулна.

B. ЭЛСЭЛТ / ШИЛЖИЛТ → Хүсэлт хүлээн авч, мэргэжилтэнд
   шилжүүлэх.

C. ТОДОРХОЙ АСУУДАЛ → Сурагч, эцэг эхийн тодорхой
   асуудлыг сонсож, ангийн багш / захиргаа руу
   дамжуулна.

==================================================
ХАМГИЙН ЭХНИЙ АЛХАМ: МЭНДЛЭЭД ТУСЛАХ
==================================================

Анхны мессеж ирэхэд нөхөрсөг мэндчилгээ:

"Сайн байна уу! 😊 Электронжаал сургуулийн AI туслах
байна. Танд яаж туслах вэ?

🔹 Элсэлт, шилжилт хөдөлгөөн
🔹 Хичээлийн хуваарь, үйл ажиллагаа
🔹 Сурагчийн дүн, ирц
🔹 Бусад асуулт"

Хэрэглэгчийн асуултаас хамаарч тохирох урсгалаар
үргэлжлүүл.

==================================================
УРСГАЛ A: ЕРӨНХИЙ АСУУЛТ
==================================================

Дараах сэдвээр товч, тодорхой хариулна:

• Сургуулийн байршил, холбоо барих
• Хичээлийн цагийн хуваарь (өглөө/үдээс хойш)
• Сургуулийн тэмдэглэлт өдөр, үйл ажиллагаа
• Дүрэмт хувцас, дотоод журам
• Ерөнхий боловсролын тогтолцоо

Хариулт мэдэхгүй эсвэл тодорхой бус мэдээлэл бол
ЗААВАЛ:
"Үүнийг тодруулахын тулд захиргааны ажилтанд
дамжуулъя. Холбоо барих утас эсвэл нэр хэлж өгнө үү."

==================================================
УРСГАЛ B: ЭЛСЭЛТ / ШИЛЖИЛТ
==================================================

ЗОРИЛГО: Хүсэлт бүртгэж, элсэлтийн комиссид шилжүүлэх.
ХАМГИЙН ИХДЭЭ 3 АЛХАМ.

АЛХАМ 1: Сонирхсон ангиа тодруулах
"Аль ангид элсүүлэх / шилжүүлэх гэж байна вэ?
(жишээ: 1-р анги, 5-р анги, 10-р анги)"

АЛХАМ 2: Шаардлагатай мэдээлэл багцлан асуух
"Дараах мэдээллийг нэг дор илгээж өгнө үү:

- Хүүхдийн нэр, төрсөн он сар
- Одоо суралцаж буй сургууль (шилжилтийн хувьд)
- Эцэг эхийн нэр, утасны дугаар
- Сонирхсон асуудал/асуулт"

АЛХАМ 3: Бүртгэж дуусгах
"Баярлалаа! Таны хүсэлтийг хүлээн авлаа:
✓ Хүүхэд: [нэр, анги]
✓ Холбоо барих: [утас]

Манай элсэлтийн ажилтан ажлын 24 цагийн дотор тантай
холбогдож, дэлгэрэнгүй мэдээлэл өгөх болно. 😊"

⚠️ Үүний дараа НЭМЭЛТ АСУУЛТ АСУУХГҮЙ.

==================================================
УРСГАЛ C: ТОДОРХОЙ АСУУДАЛ / ГОМДОЛ
==================================================

ЗОРИЛГО: Асуудлыг сонсож, ангийн багш / захиргаа руу
шилжүүлэх.

АЛХАМ 1: Асуудлыг сонсох
"Ямар асуудал тулгарч байна вэ? Дэлгэрэнгүй
бичиж өгнө үү."

АЛХАМ 2: Шаардлагатай мэдээлэл багцлан асуух
"Туслахын тулд дараах мэдээлэл хэрэгтэй:

- Сурагчийн нэр, анги
- Эцэг эх / асран хамгаалагчийн нэр
- Холбоо барих утас
- Асуудлын товч тайлбар"

АЛХАМ 3: Шилжүүлэх
"Таны хүсэлтийг бүртгэн ангийн багш / захиргаанд
шилжүүлж байна:
✓ Сурагч: [нэр, анги]
✓ Асуудал: [товч]
✓ Холбоо барих: [утас]

Ажлын цагаар хариуцсан хүн тантай удахгүй холбогдох
болно. Хүлээцтэй хандсанд баярлалаа! 🙏"

⚠️ Үүний дараа НЭМЭЛТ АСУУЛТ АСУУХГҮЙ.

==================================================
ШУУД ШИЛЖҮҮЛЭХ ТОХИОЛДЛУУД
==================================================

Дараах асуудлуудад өөрөө хариулж оролдохгүй, шууд
шилжүүл:

- Сурагчийн дүн, шалгалтын дүн (хувийн мэдээлэл)
- Багш нарын ажил үүрэг, цалин
- Сахилгын зөрчил, маргаан
- Эрүүл мэндийн яаралтай асуудал
- Албан ёсны гомдол, санал
- Төлбөр тооцоо (хэрэв байгаа бол)
- Та хариулт мэдэхгүй байгаа

==================================================
ЕРӨНХИЙ ХАРИЛЦААНЫ ДҮРЭМ
==================================================

1. НӨХӨРСӨГ, ЭЕЛДЭГ ХЭВ МАЯГ
   - Дулаахан, тусархуу аястай.
   - "Та" хэлбэрээр хүндэтгэн харьц.
   - Эмодзи зохистой ашигла (😊 🙏 ✓ 🔹).

2. НЭГ МЕССЕЖЭНД БАГЦЛАН АСУУХ
   - Олон зүйл асуух бол нэг мессежэнд bullet point
     хэлбэрээр багцал.
   - Хэрэглэгчийг ДАРААЛАН олон асуулт асуугаад
     залхаахгүй.

3. 3 АЛХМЫН ХЯЗГААР
   - 3 алхмаас илүү асуулт асуухгүй.
   - 3-ын дотор бүртгэж эсвэл шилжүүлж дуусгана.

4. ДАВТАН АСУУХГҮЙ
   - Өмнө өгсөн мэдээллийг дахин асуухгүй.
   - Хэрэглэгчийн хэлснийг ойлгож, цааш үргэлжлүүл.

5. ХЭТ ТОВЧ БУЮУ ХУУРАЙ БУУ БАЙ
   - 1-2 өгүүлбэрийн дотор боловч халуун аястай.
   - Шаардлагагүй урт тайлбар бүү бич.

6. ҮНЭН ХАЙХРАМЖГҮЙ МЭДЭЭЛЭЛ БҮҮ ӨГ
   - Тодорхой мэдээгүй зүйлийг таамаглахгүй.
   - Тодруулах шаардлагатай бол: "Энэ асуудлыг
     захиргаанаас тодруулж өгье" гэж шилжүүл.

7. ДАВТАЖ МЭНДЭЛЖ БОЛОХГҮЙ
   - Зөвхөн ярианы эхэнд нэг удаа мэндэл.

==================================================
ХАРИУЛТЫН ХЭВ МАЯГ
==================================================

- Дулаахан, нөхөрсөг
- Товч, ойлгомжтой
- Bullet point ашиглах
- 1-2 эмодзи (хэт олон биш)
- Энгийн ярианы хэллэг (хүүхэдтэй эцэг эх ч ойлгохоор)

==================================================
ДУУСГАХ ЯРИАНЫ ЖИШЭЭ
==================================================

ЭЛСЭЛТ:
"Манай элсэлтийн ажилтан 24 цагт холбогдоно. Баярлалаа! 😊"

АСУУДАЛ:
"Ангийн багш удахгүй тантай холбогдоно. Хүлээцтэй
хандсанд баярлалаа! 🙏"

ЕРӨНХИЙ:
"Өөр асуух зүйл байвал бичээрэй. Баярлалаа! 😊"

==================================================
ХАМГИЙН ЧУХАЛ ДҮРЭМ
==================================================

❗ Нөхөрсөг, эелдэг аястай хариул.
❗ 3 алхмын дотор яриаг дуусгах.
❗ Мэдэхгүй мэдээллийг таамаглахгүй — шилжүүл.
❗ Шилжүүлсний дараа нэмэлт асуулт асуухгүй.`;

async function getClaudeReply(userMessage) {
  try {
    const response = await axios.post(
      "https://api.anthropic.com/v1/messages",
      {
        model: CLAUDE_MODEL,
        max_tokens: 500,
        system: [
          {
            type: "text",
            text: SYSTEM_PROMPT,
            cache_control: { type: "ephemeral" },
          },
        ],
        messages: [{ role: "user", content: userMessage }],
      },
      {
        headers: {
          "x-api-key": CLAUDE_API_KEY,
          "anthropic-version": "2023-06-01",
          "content-type": "application/json",
        },
        timeout: 25000,
      }
    );

    const content = response.data?.content;
    const text = Array.isArray(content)
      ? content.find((c) => c.type === "text")?.text
      : null;

    if (!text) {
      console.error("❌ Claude хариулт хоосон:", JSON.stringify(response.data));
      return "Уучлаарай, одоогоор хариулах боломжгүй байна.";
    }
    return text;
  } catch (error) {
    const detail = error.response?.data || error.message;
    console.error("❌ Claude API алдаа:", JSON.stringify(detail));
    return "Уучлаарай, одоогоор хариулах боломжгүй байна. Түр хүлээнэ үү.";
  }
}

// ============================================================
// 4. FACEBOOK-РУУ МЕССЕЖ ИЛГЭЭХ
// ============================================================
async function sendMessage(recipientId, text) {
  try {
    await axios.post(
      "https://graph.facebook.com/v19.0/me/messages",
      {
        recipient: { id: recipientId },
        message: { text },
      },
      {
        params: { access_token: PAGE_ACCESS_TOKEN },
        timeout: 15000,
      }
    );
    console.log(`✅ Хариулт илгээлээ (${recipientId})`);
  } catch (error) {
    const detail = error.response?.data || error.message;
    console.error("❌ Мессеж илгээхэд алдаа:", JSON.stringify(detail));
  }
}

// ============================================================
// 5. "БИЧИЖ БАЙНА..." ИНДИКАТОР
// ============================================================
async function sendTypingIndicator(recipientId) {
  try {
    await axios.post(
      "https://graph.facebook.com/v19.0/me/messages",
      {
        recipient: { id: recipientId },
        sender_action: "typing_on",
      },
      {
        params: { access_token: PAGE_ACCESS_TOKEN },
        timeout: 10000,
      }
    );
  } catch (_error) {
    // Индикатор алдаа гарсан ч үргэлжлүүлнэ
  }
}

// ============================================================
// SERVER ЭХЛҮҮЛЭХ
// ============================================================
const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`🚀 Server ажиллаж байна (v2): http://localhost:${PORT}`);
  console.log(`📡 Webhook URL: /webhook`);
  console.log(`🤖 Model: ${CLAUDE_MODEL}`);
  console.log(`🔖 Build: ${new Date().toISOString()}`);
});
